---
name: skill-name-here
description: Use when [the trigger situation, in the words someone would actually type]. Covers [what the skill does]. Not for [the adjacent thing it should NOT fire on].
---

# Skill Name

One or two sentences: what this produces and who it is for.

## When this applies

- Concrete situation 1
- Concrete situation 2

Do not use this for X — use `other-skill` instead.

## Inputs to gather first

Before starting, establish:

1. Scope — which systems, which environment
2. Constraints — compliance regime, change windows, what must not break
3. Output format the user wants

Ask if these are not clear. Do not assume.

## Procedure

### 1. First step

What to do, concretely. Real commands where they apply:

```bash
# example
```

### 2. Second step

...

### 3. Verify

How to confirm the result is correct before presenting it. Never report success on
an unverified result.

## Output format

Describe the exact shape of the deliverable — sections, table columns, file format.

## Gotchas

- The thing that goes wrong the first time
- The assumption that is usually false

## Reference files

Put long material (control catalogues, command references, templates) in separate
files in this folder and point to them here, so they load only when needed:

- `reference/controls.md` — full control list
